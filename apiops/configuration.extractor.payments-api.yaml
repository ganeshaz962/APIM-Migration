# Limits extraction to only the Payments API and its directly-linked artifacts
# (backend, named values, policies) so it can be migrated to the destination
# workspace independently of the Orders API.
apis:
  - payments-api
